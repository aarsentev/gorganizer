TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
CALENDAR_ID=
SHEET_ID=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Local paths. The container sets these to /data/... (see Dockerfile),
# so leave them out of /data/.env on the VM.
CONFIG_PATH=config.yaml
DB_PATH=organizer.db
GOOGLE_SA_FILE=./secrets/sa.json
